import vertexSource from './shaders/vertex.glsl?raw';
import fragmentSource from './shaders/fragment.glsl?raw';

export const vertexShaderSource = vertexSource;
export const fragmentShaderSource = fragmentSource;
